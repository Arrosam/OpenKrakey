# Package: cli

Global standalone config-file management tool (UI): create/edit agent configs from a Default Plugin Setting; Krakey logo.

- Overview: [`../../overviews/nodes/cli.md`](../../overviews/nodes/cli.md)
- Implements: — · Depends on: `agent` (+ shared `config`)
- Status: **pending**

Source under `src/`. Manages config files only — does not run or talk to live agents.
