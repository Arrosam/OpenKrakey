# Package: agent_instance

Wraps ONE Agent: holds + wires clock + event-system + orchestrator + loader; start()/stop().

- Overview: [`../../overviews/nodes/agent_instance.md`](../../overviews/nodes/agent_instance.md)
- Implements: `agent` · Depends on: `clock`, `event-system`, `orchestrator`, `loader`
- Status: **pending**

Source under `src/`. Import only `../../../contracts/*` and `../../../shared/*`. Never edit contracts/ or shared/.
